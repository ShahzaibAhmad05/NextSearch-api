# Mini Search Engine (C++)

A simple search engine built in C++ that creates a lexicon, forward index, and inverted index from a CSV dataset.

## Features
- Tokenization and term normalization  
- Lexicon generation (term → TermID, doc frequency)  
- Forward index (doc → terms + positions)  
- Inverted index (term → documents + positions)

## Usage
